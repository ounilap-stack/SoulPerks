   name: Build mod

   on:
     push:
     workflow_dispatch:

   jobs:
     build:
       runs-on: ubuntu-latest
       defaults:
         run:
           working-directory: soulperks/soulperks
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-java@v4
           with:
             distribution: temurin
             java-version: 21
         - name: Build
           run: chmod +x gradlew && ./gradlew build --no-daemon
         - name: Upload jar
           uses: actions/upload-artifact@v4
           with:
             name: soulperks-jar
             path: soulperks/soulperks/build/libs/*.jar
